Flask>=3.0.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

# Optional: only needed when connecting to a real HiveServer2 cluster.
# pyhive>=0.7.0
# thrift>=0.16.0
# thrift-sasl>=0.4.3
# pure-sasl>=0.6.0

# Optional: the repo ships a lightweight local pyecharts compatibility layer.
# pyecharts>=2.0.0
