# 你负责部分说明

## 负责范围

你负责的内容是：

- MySQL 实现
- Spark / KMeans 实现
- RFM + KMeans 对照分析

---

## 一、已补齐内容

### 1. MySQL 部分

已新增：

- [docs/mysql/schema.sql](</E:/大数据/docs/mysql/schema.sql>)
- [docs/mysql/queries.sql](</E:/大数据/docs/mysql/queries.sql>)
- [docs/mysql/readme.md](</E:/大数据/docs/mysql/readme.md>)
- [scripts/load_mysql.py](</E:/大数据/scripts/load_mysql.py>)

已覆盖功能：

- 建库建表
- CSV 导入 MySQL
- 用户、订单、商品、行为四张核心表入库
- 多表关联查询示例

已实际完成验收：

- 已成功连接本机 MySQL 8.0
- 已完成 `users / products / orders / user_behaviors` 四张表导入
- 已成功执行多表关联查询
- 已导出结果文件到 [docs/outputs/mysql_acceptance](</E:/大数据/docs/outputs/mysql_acceptance>)

### 2. Spark / KMeans 部分

已新增：

- [scripts/spark_kmeans_rfm.py](</E:/大数据/scripts/spark_kmeans_rfm.py>)
- [docs/spark_kmeans_readme.md](</E:/大数据/docs/spark_kmeans_readme.md>)

已覆盖功能：

- 用户特征构造
- RFM 标签构建
- KMeans 聚类逻辑
- 轮廓系数评估逻辑
- RFM 与 KMeans 对照输出

已实际跑出结果：

- 通过多 `k` 对比后，最佳聚类数为 `3`
- 通过特征消融实验，最优轮廓系数进一步提升到：`0.3542`
- 结果文件：
  - [final_kmeans_summary.json](</E:/大数据/docs/outputs/final_kmeans_summary.json>)
  - [final_kmeans_cluster_summary.csv](</E:/大数据/docs/outputs/final_kmeans_cluster_summary.csv>)
  - [final_kmeans_rfm_vs_kmeans.csv](</E:/大数据/docs/outputs/final_kmeans_rfm_vs_kmeans.csv>)
  - [final_kmeans_cluster_labels.csv](</E:/大数据/docs/outputs/final_kmeans_cluster_labels.csv>)
  - [kmeans_compare_k_comparison.csv](</E:/大数据/docs/outputs/kmeans_compare_k_comparison.csv>)

---

## 二、当前还差什么

### 1. MySQL 当前还可继续补强的部分

- 可以补充查询结果截图，便于直接放入报告和答辩 PPT
- 可以将 MySQL 查询结果接入现有 Flask 页面，形成前后端统一展示

### 2. Spark / KMeans 还差的部分

- 还没有把聚类结果接入现有前端页面
- 当前最优轮廓系数为 `0.3542`，较初始结果提升明显，已经达到较好的课程设计实验效果，但若要进一步逼近 `0.4` 以上，仍可继续优化特征或尝试其他聚类方法

---

## 三、下一步建议

### MySQL

1. 安装并启动 MySQL
2. 安装 `pymysql`
3. 执行 `python scripts/load_mysql.py`
4. 在 MySQL 中跑 `docs/mysql/queries.sql`
5. 保存查询结果作为汇报截图

### Spark / KMeans

1. 已执行 `python scripts/spark_kmeans_rfm.py --k 4`
2. 查看 `docs/outputs/` 中输出结果
3. 下一步重点整理出：
   - 轮廓系数
   - 聚类数量
   - 各类用户特征差异
   - RFM 与 KMeans 对照结论
4. 后续继续优化特征和聚类效果，争取提升轮廓系数

---

## 四、汇报时可怎么说

可以表述为：

> 我负责的是 MySQL 与 Spark / KMeans 这条技术链，主要任务是把结构化数据入库 MySQL，并通过 Spark MLlib 完成用户聚类，再将机器学习分群结果与 RFM 规则分层进行对照分析，从而提升用户价值分层结果的可信度和技术完整性。
